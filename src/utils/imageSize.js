// import sizeOf from 'image-size';

// export const getImageDimensions = (src) => {
//   return new Promise((resolve, reject) => {
//     sizeOf(src, (err, dimensions) => {
//       if (err) reject(err);
//       resolve(dimensions);
//     });
//   });
// };
